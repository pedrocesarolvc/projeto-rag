# Variáveis de ambiente do projeto, lidas por app/config.py.
# Cada variável nasce quando a etapa que depende dela é implementada
# — nenhuma entra aqui só porque pode ser útil no futuro.

# String de conexão do PostgreSQL com a extensão pgvector habilitada.
# Necessária a partir da Etapa 4 (armazenamento de vetores).
DATABASE_URL=postgresql://usuario:senha@localhost:5432/rag

# O modelo de embedding (Etapa 4) é local — sentence-transformers,
# sem chave de API, sem dado saindo da máquina (seção 4.5).
#
# A LLM que gera a resposta (Etapa 6) também é local, via Ollama —
# nenhuma chave de API necessária. Requer o Ollama instalado e
# rodando, com o modelo baixado: `ollama pull llama3.1:8b`.

# Só para a instância pública (Hugging Face Spaces) — deixe as duas
# linhas abaixo comentadas/ausentes para rodar local com Ollama.
# LLM_PROVIDER=groq troca a geração para a API gratuita da Groq
# (console.groq.com, chave grátis, sem cartão); GROQ_API_KEY é a
# chave dessa conta.
# LLM_PROVIDER=groq
# GROQ_API_KEY=

# Assina o token de autenticação (Etapa 7). Gere uma chave aleatória
# de verdade — nunca reaproveite o valor de exemplo abaixo:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=troque-por-uma-chave-aleatoria-longa
